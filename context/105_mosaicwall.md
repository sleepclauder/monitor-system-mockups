# 105 — Страница мозаики (mosaicwall.html)

## Файл

| Файл | Назначение |
|---|---|
| `pages/mosaicwall.html` | Страница просмотра потоков в режиме мозаики |

## Назначение

Основная рабочая область для одновременного просмотра нескольких видеопотоков. Оператор перетаскивает объекты мониторинга из левой панели в область просмотра — формируя произвольную раскладку «мозаику». Раскладки можно сохранять как закладки и восстанавливать в один клик.

Данные объектов синхронизированы с `pages/objects.html` (те же 8 объектов, теги, статусы).

## Структура страницы

```
┌──────────┬──────────────────────┬──────────────────────────────────────┐
│ Sidebar  │  Панель каналов      │  Toolbar: заголовок + Закладки + ✕   │
│  (72px)  │  (300px, сворач.)    │──────────────────────────────────────│
│          │──────────────────────│                                      │
│          │  [Поиск…           ] │                                      │
│          │──────────────────────│    Область плееров (grid)            │
│          │  Все  Live  HLS  …   │                                      │
│          │──────────────────────│    ┌──────────┐  ┌──────────┐        │
│          │  Название  │  Теги   │    │  📺 Ch1  │  │  📺 Ch3  │        │
│          │  ──────────┼──────   │    └──────────┘  └──────────┘        │
│          │  TV Ch 1   │ HLS L   │                                      │
│          │  FM 101.5  │ MP-TS   │    ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐       │
│          │  ...       │ ...     │      ⊞ Перетащите объект...           │
│          │                      │    └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘       │
└──────────┴──────────────────────┴──────────────────────────────────────┘
```

---

## 1. Панель объектов (.channel-panel)

Сворачиваемая панель шириной **300px** слева от основной области. Содержит список объектов мониторинга с поиском и фильтрацией по тегам.

### Состояния панели

| Состояние | Описание |
|---|---|
| Открыта (по умолчанию) | Ширина 300px, видны кнопка ◀ в заголовке |
| Свёрнута | Ширина 0, плавная анимация 0.25s ease, в тулбаре появляется кнопка «▶ Каналы» |

- Кнопка ◀ в заголовке панели — сворачивает
- Кнопка «▶ Каналы» в тулбаре — разворачивает

### Поиск

Текстовое поле. Фильтрует строки таблицы в реальном времени (`input`-событие) по полю `name` (case-insensitive). Работает совместно с фильтром по тегам.

### Фильтр по тегам (.tag-filters)

Строка чипов-кнопок над таблицей.

| Чип | data-tag | Цвет активного состояния |
|---|---|---|
| Все | `all` | белый (rgba(255,255,255,0.1)) |
| Live | `Live` | зелёный (accent-primary) |
| HLS | `HLS` | голубой (accent-secondary) |
| MPEG-TS | `MPEG-TS` | фиолетовый (#a78bfa) |
| VOD | `VOD` | серый (text-secondary) |

**Логика мультиселекта (OR):**
- Можно выбрать несколько тегов одновременно — каждый подсвечивается своим цветом
- Показываются объекты, у которых есть **хотя бы один** из выбранных тегов
- Клик по уже активному тегу — снимает его выделение
- «Все» — сбрасывает все выбранные теги, показывает полный список
- Если активных тегов нет — «Все» подсвечивается автоматически

### Таблица объектов (.channel-table)

Двухколоночная таблица с фиксированной высотой строк.

| Параметр | Значение |
|---|---|
| Высота строки | 36px (фиксированная) |
| Ширина колонки «Название» | 60% |
| Ширина колонки «Теги» | 40% |
| Перенос тегов | Отключён (nowrap), лишние скрываются |
| Заголовок | Sticky (top: 0), z-index: 1 |

**Колонка «Название»:** цветная точка статуса + текст с ellipsis при переполнении.

**Колонка «Теги»:** цветные таблетки-пилюли, соответствующие тегам объекта.

#### Статусы объектов

| Статус | Цвет точки | Описание |
|---|---|---|
| `online` | зелёный (accent-primary, свечение) | Поток активен |
| `warning` | оранжевый (accent-warning, свечение) | Поток с предупреждением |
| `offline` | серый (text-secondary) | Поток недоступен |

#### Цвета тег-пилюль в таблице

| Тег | CSS-класс | Цвет |
|---|---|---|
| Live | `.tag-pill--live` | зелёный (accent-primary) |
| HLS | `.tag-pill--hls` | голубой (accent-secondary) |
| MPEG-TS | `.tag-pill--mpegts` | фиолетовый (#a78bfa) |
| VOD | `.tag-pill--vod` | серый (text-secondary) |

#### Drag & Drop из таблицы

- Каждая строка `draggable="true"`
- `dragstart` — записывает `channelId` в переменную `dragChannelId`, добавляет класс `.dragging` (opacity: 0.35)
- `dragend` — снимает класс, сбрасывает `dragChannelId`

---

## 2. Тулбар (.toolbar)

Фиксированная полоса высотой **52px** над областью плееров.

| Элемент | Расположение | Описание |
|---|---|---|
| «▶ Каналы» | toolbar-left | Появляется только когда панель свёрнута. Разворачивает панель |
| Заголовок «Мозаика» | toolbar-left | Статический текст, 16px, font-weight: 600 |
| «🔖 Закладки ▾» | toolbar-right | Кнопка-dropdown управления раскладками |
| «✕ Очистить всё» | toolbar-right | btn-danger, удаляет все плееры из сетки |

---

## 3. Область плееров (.player-area)

Основная рабочая зона, занимает всё оставшееся пространство. Прокручиваемая (`overflow: auto`).

### Drop-зона

- `dragover` — подсвечивает фон (rgba(0,255,136,0.03)), разрешает drop
- `dragleave` — снимает подсветку при выходе за границы области
- `drop` — вызывает `addPlayer(dragChannelId)`, добавляет новый плеер

### Сетка плееров (.player-grid)

```css
grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
gap: 12px;
```

Сетка автоматически адаптируется под ширину области. При добавлении плееров они выстраиваются слева направо, сверху вниз.

### Подсказка (.drop-hint)

Символ `⊞` + текст «Перетащите объект для просмотра». Позиционируется внизу по центру области (absolute, bottom: 24px).

| Состояние | Видимость |
|---|---|
| Нет плееров | Видна (opacity: 1) |
| Есть ≥1 плеер | Скрыта (opacity: 0, класс `.hidden`) |

---

## 4. Плеер (.player-cell)

Каждый добавленный объект отображается как плеер с соотношением сторон **16:9**.

### Визуальная структура плеера

```
┌────────────────────────────────────┐  ← border: 1px border-color
│  [⛶] [✕]                          │  ← .player-controls (видны при hover)
│                                    │
│          📺  (opacity: 0.12)       │  ← имитация видеопотока
│    (уникальный цветовой градиент)  │     (radial-gradient по цвету канала)
│    (горизонтальные scanlines)      │
│                                    │
│  Название канала    [Live][HLS]    │  ← .player-info (gradient overlay снизу)
└────────────────────────────────────┘
```

### Имитация видеопотока

Фон генерируется по уникальному цвету канала (`channel.color`):
```
radial-gradient(ellipse at 30% 40%, {color}18 0%, transparent 60%),
radial-gradient(ellipse at 70% 70%, {color}10 0%, transparent 50%),
#050810
```
Поверх — псевдоэлемент `::after` с горизонтальными полосами (scanlines, rgba(0,0,0,0.08)).

### Кнопки управления плеером (.player-controls)

Скрыты по умолчанию (opacity: 0), появляются при наведении на плеер.

| Кнопка | Класс | Действие |
|---|---|---|
| ⛶ | `.player-btn-fs` | Открывает полноэкранный оверлей для этого канала |
| ✕ | `.player-btn-close` | Удаляет плеер из сетки (`removePlayer(playerId)`) |

### Информационный оверлей (.player-info)

Расположен в нижней части плеера поверх градиентной тени (transparent → rgba(0,0,0,0.8)).

| Элемент | Описание |
|---|---|
| Название канала | font-size: 11px, font-weight: 600, text-shadow, ellipsis |
| Теги-бейджи | Цветные бейджи: `Live` (красный), `HLS` (голубой), `MPEG-TS` (фиолетовый), `VOD` (серый) |

### Hover-состояние плеера

- Рамка: rgba(0,255,136,0.3)
- Box-shadow: 0 0 16px rgba(0,255,136,0.08)

---

## 5. Полноэкранный оверлей (.fs-overlay)

Занимает весь экран (position: fixed, inset: 0, z-index: 1000).

| Элемент | Описание |
|---|---|
| Toolbar (absolute, top) | Название канала + кнопка «✕ Закрыть» |
| Область видео | Тот же градиентный фон канала, что и в плеере, но на весь экран |

**Открытие:** кнопка ⛶ на плеере → `openFullscreen(channel)`
**Закрытие:** кнопка «✕ Закрыть» или клавиша `Escape`

---

## 6. Закладки (Dropdown «🔖 Закладки ▾»)

Управление сохранёнными раскладками. Данные хранятся в `localStorage` по ключу `monix_layouts`.

### Структура дропдауна

```
┌──────────────────────────┐
│  💾 Сохранить раскладку  │  ← всегда
├──────────────────────────┤
│  СОХРАНЁННЫЕ             │
│  📐 Утро 08:00  5 объ. 🗑 │
│  📐 Смена 2    3 объ. 🗑  │
└──────────────────────────┘
```

| Действие | Описание |
|---|---|
| «💾 Сохранить раскладку» | Если плееров нет — alert. Иначе prompt для ввода имени → сохраняет `{name, channels: [channelId,...], ts}` |
| Клик по раскладке | `clearAll()` + восстанавливает плееры по сохранённым `channelId` |
| 🗑 | Удаляет раскладку из localStorage, перерисовывает список |

**Открытие/закрытие:** клик по кнопке «Закладки ▾». Закрывается при клике вне дропдауна (`document.click`).

---

## 7. Данные объектов (CHANNELS)

Объекты синхронизированы с `pages/objects.html`. Любые изменения в списке объектов должны вноситься в оба файла.

| id | Название | Теги | Статус | Цвет |
|---|---|---|---|---|
| 1 | TV Channel 1 — Main Feed | HLS, Live | online | #00ff88 |
| 2 | TV Channel 2 — Sports | HLS, Live | online | #00ccff |
| 3 | Radio Stream — FM 101.5 | MPEG-TS, Live | online | #a78bfa |
| 4 | Test Stream — QA | HLS, Live, VOD | warning | #ffaa00 |
| 5 | Backup Feed — Channel 1 | MPEG-TS | offline | #8b94a8 |
| 6 | Dev Environment Stream | HLS, VOD | offline | #fb923c |
| 7 | TV Channel 3 — News 24/7 | HLS, Live | online | #f472b6 |
| 8 | IPTV Multicast — Region A | MPEG-TS, Live | online | #60a5fa |

---

## 8. Связи с другими страницами проекта

| Страница | Связь |
|---|---|
| `objects.html` | Источник данных объектов (имена, теги). Цветовая схема тегов идентична |
| `settings.html` | Управление пользователями и группами (вкладка Users). Косвенная связь: группы определяют доступ к объектам |
| `components/sidebar.js` | Mosaicwall добавлена в `navItems` с иконкой ⊞ (U+229E) между объектами и настройками |

### Навигация из mosaicwall

Переходы на другие страницы — только через основной sidebar (стандартное поведение).

---

## 9. Стилевые параметры

### CSS-переменные (dark theme — идентичны остальным страницам)

| Переменная | Значение |
|---|---|
| `--bg-primary` | #0a0e1a |
| `--bg-secondary` | #111827 |
| `--bg-tertiary` | #1a2332 |
| `--accent-primary` | #00ff88 |
| `--accent-secondary` | #00ccff |
| `--accent-warning` | #ffaa00 |
| `--accent-danger` | #ff3366 |
| `--text-primary` | #e4e8f0 |
| `--text-secondary` | #8b94a8 |
| `--border-color` | #1f2937 |

### Ключевые размеры

| Параметр | Значение |
|---|---|
| Ширина sidebar | 72px |
| Ширина панели каналов | 300px (0 в свёрнутом состоянии) |
| Высота тулбара | 52px |
| Высота строки таблицы | 36px |
| Мин. ширина плеера в сетке | 300px |

### z-index карта

| z-index | Элемент |
|---|---|
| 10 | Панель каналов (.channel-panel) |
| 100 | Sidebar |
| 200 | Dropdown «Закладки» |
| 1000 | Полноэкранный оверлей (.fs-overlay) |

---

## 10. Подключение

```html
<aside class="sidebar"></aside>
<!-- ... основной контент ... -->
<script src="../components/sidebar.js"></script>
<script>
    // Вся логика страницы: inline-скрипт в конце body
    // Состояние: players[], activeTags (Set), searchQuery, dragChannelId
    // Хранилище: localStorage['monix_layouts']
</script>
```
