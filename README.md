# MONITOR//SYS - Система Мониторинга IT-Инфраструктуры

Интерактивные HTML мокапы системы мониторинга серверов, сетей и приложений с темной темой для круглосуточной работы.

![Version](https://img.shields.io/badge/version-1.0.0-brightgreen)
![Status](https://img.shields.io/badge/status-mockup-blue)

## 📋 Описание

Полнофункциональные HTML/CSS мокапы системы мониторинга, готовые для передачи дизайнерам и фронтенд-разработчикам. Все страницы выполнены в единой стилистике с темной темой и неоновыми акцентами.

## 🎨 Особенности дизайна

- **Темная тема** - оптимизирована для длительной работы и снижения нагрузки на глаза
- **Неоновые акценты** - зеленый (#00ff88) и синий (#00ccff) для выделения важных элементов
- **Типографика** - комбинация шрифтов Outfit и JetBrains Mono для современного вида
- **Анимации** - плавные переходы и эффекты появления элементов
- **Интерактивность** - hover-эффекты, анимированные индикаторы состояния

## 📁 Структура проекта

```
monitor-system-mockups/
├── pages/                      # HTML страницы
│   ├── dashboard.html          # Главная панель
│   ├── objects.html            # Список объектов мониторинга
│   ├── object-detail.html      # Детальная информация об объекте
│   ├── alerts.html             # Алерты и уведомления
│   ├── reports.html            # Отчеты и графики
│   └── settings.html           # Настройки системы
├── assets/                     # Ресурсы (для будущего использования)
├── docs/                       # Документация
└── README.md                   # Этот файл
```

## 🚀 Быстрый старт

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd monitor-system-mockups
```

2. Откройте любую страницу в браузере:
```bash
# На macOS
open pages/dashboard.html

# На Linux
xdg-open pages/dashboard.html

# На Windows
start pages/dashboard.html
```

### Запуск с локальным сервером

Для корректной работы навигации между страницами рекомендуется использовать локальный веб-сервер:

**Python 3:**
```bash
cd pages
python3 -m http.server 8000
```

**Node.js (с помощью http-server):**
```bash
npm install -g http-server
cd pages
http-server -p 8000
```

**PHP:**
```bash
cd pages
php -S localhost:8000
```

Затем откройте в браузере: `http://localhost:8000/dashboard.html`

## 📄 Страницы

### 1. Dashboard (dashboard.html)
**Главная панель управления**
- Статистика в реальном времени (активные серверы, предупреждения, ошибки)
- Графики производительности CPU
- Статусы систем с индикаторами
- Таблица последней активности

### 2. Объекты мониторинга (objects.html)
**Список всех мониторируемых объектов**
- Фильтры по статусу, типу, локации
- Карточки серверов с метриками (CPU, Memory, Disk, Network)
- Визуальные индикаторы состояния
- Пагинация

### 3. Детали объекта (object-detail.html)
**Подробная информация о сервере**
- Ключевые метрики с графиками
- Список активных процессов
- Системная и сетевая информация
- Системные логи в реальном времени

### 4. Алерты и уведомления (alerts.html)
**Управление событиями и инцидентами**
- Статистика по типам алертов
- Фильтрация по критичности
- Детальные карточки событий
- Действия над алертами (просмотр, решение)

### 5. Отчеты и графики (reports.html)
**Аналитика и визуализация данных**
- Настраиваемые фильтры периода
- Множественные графики производительности
- Таблица с метриками серверов
- Шаблоны готовых отчетов

### 6. Настройки (settings.html)
**Конфигурация системы**
- Основные параметры
- Пороги алертов
- Каналы уведомлений (Email, Slack, Telegram, PagerDuty)
- API ключи
- Настройки хранения данных

## 🎯 Использование

### Для дизайнеров
1. Откройте HTML файлы в браузере для просмотра интерфейса
2. Изучите цветовую палитру и типографику в CSS (`:root` переменные)
3. Используйте DevTools браузера для извлечения стилей
4. Модифицируйте CSS напрямую в файлах для экспериментов

### Для разработчиков
1. Все стили встроены в `<style>` теги для удобства
2. CSS переменные в `:root` для быстрой кастомизации
3. Semantic HTML разметка
4. Готово для интеграции с JavaScript фреймворками

## 🛠️ Технологии

- **HTML5** - семантическая разметка
- **CSS3** - современные стили с flexbox/grid
- **Google Fonts** - Outfit и JetBrains Mono
- **CSS Variables** - для легкой кастомизации темы
- **CSS Animations** - плавные переходы и эффекты

## 🎨 Цветовая палитра

```css
--bg-primary: #0a0e1a;          /* Основной фон */
--bg-secondary: #111827;        /* Фон карточек */
--bg-tertiary: #1a2332;         /* Фон элементов */
--accent-primary: #00ff88;      /* Основной акцент (зеленый) */
--accent-secondary: #00ccff;    /* Вторичный акцент (синий) */
--accent-warning: #ffaa00;      /* Предупреждение (оранжевый) */
--accent-danger: #ff3366;       /* Ошибка (красный) */
--text-primary: #e4e8f0;        /* Основной текст */
--text-secondary: #8b94a8;      /* Вторичный текст */
--border-color: #1f2937;        /* Цвет границ */
```

## 📝 Модификация

### Изменение цветов
Отредактируйте CSS переменные в секции `:root` любого HTML файла:

```css
:root {
    --accent-primary: #00ff88;  /* Измените на ваш цвет */
}
```

### Изменение шрифтов
Замените импорт Google Fonts в `<head>`:

```html
<link href="https://fonts.googleapis.com/css2?family=YourFont:wght@400;600;700&display=swap" rel="stylesheet">
```

И обновите CSS:

```css
body {
    font-family: 'YourFont', sans-serif;
}
```

## 🔄 Интеграция с бэкендом

Мокапы готовы для интеграции с реальными данными:

1. **Замените статические данные** на AJAX/Fetch запросы
2. **Графики** - используйте Chart.js, D3.js или Plotly для визуализации
3. **Реальное время** - WebSockets для live-обновлений
4. **Аутентификация** - добавьте страницу логина и JWT токены

### Пример интеграции (JavaScript)

```javascript
// Получение данных серверов
async function fetchServers() {
    const response = await fetch('/api/servers');
    const servers = await response.json();
    renderServers(servers);
}

// Обновление в реальном времени
const ws = new WebSocket('ws://your-api/realtime');
ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    updateMetrics(data);
};
```

## 📱 Адаптивность

Текущие мокапы оптимизированы для desktop разрешений (1920x1080+). Для адаптации под мобильные устройства рекомендуется:

- Добавить media queries для различных breakpoints
- Сделать sidebar сворачиваемым
- Адаптировать grid-layout для мобильных экранов
- Оптимизировать размеры шрифтов и padding

## 🤝 Внесение изменений

1. Создайте ветку для ваших изменений:
```bash
git checkout -b feature/your-feature-name
```

2. Внесите изменения в HTML/CSS файлы

3. Коммитьте изменения:
```bash
git add .
git commit -m "Описание ваших изменений"
```

4. Отправьте изменения:
```bash
git push origin feature/your-feature-name
```

5. Создайте Pull Request для review

## 📋 TODO / Roadmap

- [ ] Добавить адаптивный дизайн для мобильных устройств
- [ ] Создать реальные графики с Chart.js
- [ ] Добавить темную/светлую тему (toggle)
- [ ] Создать компонентную библиотеку (React/Vue версия)
- [ ] Добавить больше анимаций и микро-взаимодействий
- [ ] Создать страницу логина/аутентификации
- [ ] Добавить экспорт данных в PDF/Excel
- [ ] Интеграция с реальным API

## 📄 Лицензия

MIT License - свободно используйте в ваших проектах

## 👥 Команда

Разработано для команды мониторинга IT-инфраструктуры

---

**Последнее обновление:** 7 февраля 2026

**Версия:** 1.0.0

Для вопросов и предложений создавайте Issues в репозитории.
