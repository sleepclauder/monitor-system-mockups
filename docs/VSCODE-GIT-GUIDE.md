# Загрузка проекта на GitHub через VS Code

Пошаговое руководство для загрузки мокапов системы мониторинга на GitHub используя Visual Studio Code.

## 📋 Содержание
1. [Подготовка](#подготовка)
2. [Установка VS Code](#установка-vs-code)
3. [Установка Git](#установка-git)
4. [Настройка Git](#настройка-git)
5. [Открытие проекта в VS Code](#открытие-проекта-в-vs-code)
6. [Создание репозитория на GitHub](#создание-репозитория-на-github)
7. [Инициализация Git в VS Code](#инициализация-git-в-vs-code)
8. [Первый коммит](#первый-коммит)
9. [Публикация на GitHub](#публикация-на-github)
10. [Работа с изменениями](#работа-с-изменениями)
11. [Решение проблем](#решение-проблем)

---

## Подготовка

### Что вам понадобится:

- ✅ Аккаунт на GitHub (уже есть)
- ✅ Папка с проектом `monitor-system-mockups` (скачайте и распакуйте архив)
- ⏳ Visual Studio Code (установим)
- ⏳ Git (установим)

---

## Установка VS Code

### Windows:
1. Перейдите на https://code.visualstudio.com/
2. Нажмите "Download for Windows"
3. Запустите установщик
4. **Важные галочки при установке:**
   - ✅ Add "Open with Code" to context menu
   - ✅ Add to PATH
5. Нажимайте "Next" → "Install"

### macOS:
1. Перейдите на https://code.visualstudio.com/
2. Нажмите "Download for macOS"
3. Откройте скачанный .zip
4. Перетащите VS Code в папку Applications
5. Запустите из Applications

### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install software-properties-common apt-transport-https wget
wget -q https://packages.microsoft.com/keys/microsoft.asc -O- | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://packages.microsoft.com/repos/vscode stable main"
sudo apt update
sudo apt install code
```

### Проверка установки:
Запустите VS Code - должно открыться окно приветствия.

---

## Установка Git

### Windows:
1. Скачайте с https://git-scm.com/download/win
2. Запустите установщик
3. **Важные настройки:**
   - Редактор: "Use Visual Studio Code as Git's default editor"
   - PATH: "Git from the command line and also from 3rd-party software"
   - Line endings: "Checkout Windows-style, commit Unix-style"
   - Terminal: "Use Windows' default console window"
   - Default branch: "Override" → `main`
4. Завершите установку

### macOS:
```bash
# Через Homebrew (рекомендуется)
brew install git

# Или скачайте с https://git-scm.com/download/mac
```

### Linux:
```bash
sudo apt-get update
sudo apt-get install git
```

### Проверка:
Откройте терминал/командную строку:
```bash
git --version
```
Должна появиться версия Git (например: `git version 2.42.0`)

---

## Настройка Git

**Это нужно сделать только один раз!**

### Способ 1: Через терминал VS Code

1. Откройте VS Code
2. Нажмите **View → Terminal** (или Ctrl+`)
3. В терминале внизу выполните (подставьте свои данные):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

**⚠️ Важно:** Email должен совпадать с вашим GitHub email!

**Пример:**
```bash
git config --global user.name "Ivan Petrov"
git config --global user.email "ivan.petrov@gmail.com"
```

### Способ 2: Через интерфейс VS Code

Это настроится автоматически при первом коммите.

---

## Открытие проекта в VS Code

### Шаг 1: Распакуйте архив

1. Скачайте `monitor-system-mockups.zip` (который я создал)
2. Распакуйте в удобное место:
   - **Windows:** `C:\Users\ВАШ_USER\Projects\monitor-system-mockups`
   - **macOS/Linux:** `~/Projects/monitor-system-mockups`

### Шаг 2: Откройте папку в VS Code

**Вариант A - Через меню:**
1. Запустите VS Code
2. **File → Open Folder...** (или **File → Open...** на macOS)
3. Выберите папку `monitor-system-mockups`
4. Нажмите "Select Folder" / "Open"

**Вариант B - Через проводник:**
1. Откройте папку `monitor-system-mockups` в проводнике
2. Правый клик на папке → **"Open with Code"**
   (если эта опция есть)

**Вариант C - Через терминал:**
```bash
cd /путь/к/папке/monitor-system-mockups
code .
```

### Шаг 3: Убедитесь что открыли правильную папку

В боковой панели (Explorer) вы должны видеть:
```
MONITOR-SYSTEM-MOCKUPS
├── 📁 pages
├── 📁 docs
├── 📁 assets
├── 📄 index.html
├── 📄 README.md
├── 📄 package.json
└── ...
```

Если видите только один файл - вы открыли файл, а не папку. Закройте и откройте папку целиком.

---

## Создание репозитория на GitHub

**Это делается через браузер, НЕ в VS Code.**

### Шаг 1: Войдите на GitHub
1. Откройте https://github.com
2. Войдите в аккаунт (Sign in)

### Шаг 2: Создайте новый репозиторий
1. Нажмите зеленую кнопку **"New"** (или "+" → New repository)
2. Заполните форму:

```
Repository name: monitor-system-mockups
Description: Интерактивные HTML мокапы системы мониторинга IT-инфраструктуры
```

**Public или Private:** выберите что предпочитаете

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- ❌ **НЕ ставьте** галочку "Add a README file"
- ❌ **НЕ ставьте** галочку "Add .gitignore"
- ❌ **НЕ ставьте** галочку "Choose a license"

(У вас уже есть все эти файлы!)

3. Нажмите **"Create repository"**

### Шаг 3: Не закрывайте эту страницу!

После создания вы увидите страницу с инструкциями.
**Держите её открытой** - она понадобится через минуту.

---

## Инициализация Git в VS Code

Теперь возвращаемся в VS Code.

### Шаг 1: Откройте Source Control

**Способ 1:** Нажмите на иконку веточки 🌿 в левой панели (3-я сверху)

**Способ 2:** View → Source Control (или Ctrl+Shift+G / Cmd+Shift+G)

### Шаг 2: Инициализируйте репозиторий

Вы увидите большую синюю кнопку **"Initialize Repository"**

![Initialize Repository Button](https://code.visualstudio.com/assets/docs/editor/versioncontrol/overview.png)

**Нажмите на неё!**

### Что произошло:

- ✅ Создана скрытая папка `.git` в вашем проекте
- ✅ VS Code теперь отслеживает изменения
- ✅ Вы увидите список всех файлов в секции "Changes"

Вы увидите много файлов с буквой **U** (Untracked) - это нормально!

---

## Первый коммит

Теперь сохраним все файлы в истории Git.

### Шаг 1: Добавьте все файлы

В панели **Source Control** вы видите список файлов под **"Changes"**.

**Нажмите на кнопку "+"** рядом с **"Changes"** (при наведении появится подсказка "Stage All Changes")

![Stage All Changes](https://code.visualstudio.com/assets/docs/editor/versioncontrol/scm-stage-changes.png)

Все файлы переместятся в секцию **"Staged Changes"**.

**Что это значит:**
- **Changes** = изменённые файлы
- **Staged Changes** = файлы готовые к сохранению (коммиту)

### Шаг 2: Напишите commit message

Вверху панели Source Control есть текстовое поле с подсказкой **"Message"**.

Нажмите на него и напишите:
```
Initial commit: все страницы мокапов v1.0.0
```

### Шаг 3: Создайте коммит

**Способ 1:** Нажмите **Ctrl+Enter** (Cmd+Enter на macOS)

**Способ 2:** Нажмите галочку ✓ над текстовым полем (подсказка "Commit")

**Способ 3:** Нажмите кнопку **"Commit"** в выпадающем меню

### Результат:

Все файлы исчезнут из списка Changes - это значит коммит создан! ✅

---

## Публикация на GitHub

Самый важный момент - загрузка на GitHub!

### Шаг 1: Нажмите "Publish Branch"

После коммита вверху панели Source Control появится синяя кнопка:

**"Publish Branch"** ☁️

**Нажмите на неё!**

### Шаг 2: Выберите куда публиковать

Появится окно вверху с двумя вариантами:

```
🌐 Publish to GitHub public repository
🔒 Publish to GitHub private repository
```

Выберите нужный вариант (в зависимости от того что выбрали при создании репозитория на GitHub).

### Шаг 3: Войдите в GitHub (если попросит)

Если вы ещё не авторизованы в VS Code:

1. Появится окно **"Sign in to GitHub"**
2. Нажмите **"Allow"**
3. Откроется браузер
4. Нажмите **"Continue"**
5. Нажмите **"Authorize Visual-Studio-Code"**
6. Введите пароль GitHub если попросит
7. Вернитесь в VS Code

### Шаг 4: Выберите имя репозитория

VS Code предложит имя. Используйте:
```
monitor-system-mockups
```

Нажмите **Enter**.

### Шаг 5: Подождите загрузки

Внизу справа появится индикатор прогресса:
```
Syncing... (uploading)
```

Это может занять 10-30 секунд.

### ✅ Готово!

Когда загрузка завершится, вы увидите уведомление:
```
Successfully published branch 'main'
```

---

## Проверка результата

### Откройте GitHub в браузере

1. Перейдите на https://github.com/ВАШ_USERNAME/monitor-system-mockups
2. Вы должны увидеть все ваши файлы!
3. Нажмите на файлы - можете посмотреть содержимое

**Поздравляю! Вы загрузили проект на GitHub!** 🎉

---

## Работа с изменениями

Теперь научимся вносить изменения и загружать их.

### Сценарий: Изменим текст

### Шаг 1: Откройте файл

В VS Code:
1. Откройте **Explorer** (иконка файлов, 1-я сверху)
2. Откройте `pages` → `dashboard.html`

### Шаг 2: Внесите изменение

Найдите строку (примерно 345):
```html
<h2>Общий обзор системы</h2>
```

Измените на:
```html
<h2>Мониторинг IT-инфраструктуры</h2>
```

Сохраните: **Ctrl+S** (Cmd+S)

### Шаг 3: Посмотрите изменения

1. Откройте **Source Control** (иконка веточки)
2. Вы увидите `dashboard.html` с буквой **M** (Modified)
3. Нажмите на файл - справа откроется **diff** (сравнение):
   - **Красный** = старая строка
   - **Зеленый** = новая строка

### Шаг 4: Stage и Commit

1. Нажмите **"+"** рядом с файлом (или "Stage All Changes")
2. Напишите commit message:
```
Обновлён заголовок на главной странице
```
3. Нажмите **✓ Commit**

### Шаг 5: Загрузите на GitHub

Вверху панели Source Control появится кнопка:

**"Sync Changes"** ⤴️⤵️

**Нажмите на неё!**

Это одновременно:
- ⤵️ Скачает изменения коллег (git pull)
- ⤴️ Загрузит ваши изменения (git push)

### ✅ Готово!

Обновите страницу репозитория на GitHub - изменения там!

---

## Ежедневная работа (краткая версия)

```
1. Откройте проект в VS Code
2. Source Control → Sync (⤴️⤵️) - получить изменения коллег
3. Редактируйте файлы
4. Source Control → Stage Changes (+)
5. Напишите commit message
6. Commit (✓)
7. Sync Changes (⤴️⤵️)
```

---

## Полезные функции VS Code

### 1. Git Graph - визуализация истории

**Установка:**
1. Extensions (иконка кубиков) или Ctrl+Shift+X
2. Найдите "Git Graph"
3. Install

**Использование:**
- Source Control → ... → View Git Graph
- Увидите красивое дерево коммитов

### 2. GitLens - расширенный Git

**Установка:**
1. Extensions → найдите "GitLens"
2. Install

**Что даёт:**
- Кто и когда изменил каждую строку
- История изменений файла
- Сравнение веток

### 3. Встроенный терминал

**Ctrl+`** (или View → Terminal)

Можно выполнять любые git команды:
```bash
git status
git log
git pull
```

### 4. Автосохранение

**File → Auto Save** ✅

Файлы будут сохраняться автоматически.

---

## Решение проблем

### ❌ Кнопка "Publish Branch" не появилась

**Причина:** Возможно уже создан remote.

**Решение:**
1. Откройте терминал (Ctrl+`)
2. Выполните:
```bash
git remote -v
```

Если пусто - добавьте вручную:
```bash
git remote add origin https://github.com/ВАШ_USERNAME/monitor-system-mockups.git
git push -u origin main
```

### ❌ "Authentication failed"

**Причина:** Неправильный пароль или токен.

**Решение:**
1. Создайте Personal Access Token:
   - GitHub → Settings → Developer settings → Personal access tokens
   - Generate new token (classic)
   - Выберите `repo` права
   - Скопируйте токен (ghp_...)

2. В VS Code:
   - Command Palette (Ctrl+Shift+P / Cmd+Shift+P)
   - Наберите "Git: Clone"
   - Используйте токен вместо пароля

Или настройте Git Credential Manager:
```bash
git config --global credential.helper store
```

### ❌ "Repository not found"

**Причина:** Неправильный URL репозитория.

**Решение:**
1. Проверьте URL на GitHub
2. В терминале VS Code:
```bash
git remote set-url origin https://github.com/ПРАВИЛЬНЫЙ_USERNAME/monitor-system-mockups.git
```

### ❌ "Failed to push some refs"

**Причина:** На GitHub есть изменения которых нет у вас.

**Решение:**
```bash
git pull origin main --rebase
git push origin main
```

Или через VS Code:
- Source Control → ... → Pull, Rebase
- Потом Sync

### ❌ Конфликт при Sync

**Что произошло:** Вы и коллега изменили одну строку.

**Решение:**

1. VS Code покажет конфликтующие файлы с **!**
2. Откройте файл - увидите:

```html
<<<<<<< HEAD (Current Change)
<h2>Ваша версия</h2>
=======
<h2>Версия коллеги</h2>
>>>>>>> abc123 (Incoming Change)
```

3. Над конфликтом есть кнопки:
   - **Accept Current Change** - оставить вашу версию
   - **Accept Incoming Change** - взять версию коллеги
   - **Accept Both Changes** - оставить обе строки
   - **Compare Changes** - посмотреть детально

4. Выберите нужный вариант
5. Сохраните файл
6. Source Control → Stage → Commit → Sync

### ❌ Git не видит изменения

**Решение:**
1. Убедитесь что сохранили файл (Ctrl+S)
2. Проверьте `.gitignore` - может файл исключён
3. Перезапустите VS Code

---

## Приглашение команды

### Через GitHub:

1. Откройте ваш репозиторий на GitHub
2. Settings → Collaborators
3. Add people
4. Введите username коллеги
5. Выберите роль (Write для разработчиков)

### Коллега клонирует проект:

**В VS Code:**
1. Command Palette (Ctrl+Shift+P / Cmd+Shift+P)
2. Git: Clone
3. Вставьте URL: `https://github.com/ВАШ_USERNAME/monitor-system-mockups.git`
4. Выберите папку куда сохранить
5. "Open" когда клонирование завершится

Готово! Теперь команда может работать вместе.

---

## Горячие клавиши VS Code для Git

```
Ctrl+Shift+G (Cmd+Shift+G)  - Source Control
Ctrl+Enter (Cmd+Enter)      - Commit
Ctrl+` (Cmd+`)              - Terminal
Ctrl+Shift+P (Cmd+Shift+P)  - Command Palette
Ctrl+K Ctrl+O               - Open Folder
```

---

## Чек-лист: Что должно быть настроено

✅ VS Code установлен
✅ Git установлен
✅ `git config --global user.name` настроен
✅ `git config --global user.email` настроен
✅ Проект открыт в VS Code как папка
✅ Git инициализирован (есть Source Control)
✅ Репозиторий создан на GitHub
✅ VS Code авторизован в GitHub
✅ Проект опубликован на GitHub

---

## Дополнительные ресурсы

- [VS Code Git Documentation](https://code.visualstudio.com/docs/editor/versioncontrol)
- [VS Code Tips and Tricks](https://code.visualstudio.com/docs/getstarted/tips-and-tricks)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)

---

**Готово!** Теперь вы можете работать с Git через удобный интерфейс VS Code без команд в терминале! 🎉

При возникновении вопросов - смотрите раздел "Решение проблем" или создайте Issue на GitHub.
