# 104 — Страница настроек (settings.html)

## Файл

| Файл | Назначение |
|---|---|
| `pages/settings.html` | Страница управления пользователями, группами и тегами |

## Назначение

Единая страница администрирования системы. Организована в виде вкладок (tabs) верхнего уровня: **USERS**, **GROUPS**, **TAGS**. Каждая вкладка — самостоятельный раздел с собственным интерфейсом.

Связанные требования:
- `docs/104_settings_main.md` — общее описание
- `docs/104_settings_users.md` — требования к вкладке Users
- `docs/104_settings_groups.md` — требования к вкладке Groups
- `docs/104_settings_tags.md` — требования к вкладке Tags

## Общая структура страницы

```
┌──────────────────────────────────────────────────────────┐
│  Sidebar │  Header: «Настройки» + кнопка действия        │
│  (72px)  │───────────────────────────────────────────────│
│          │  Вкладки:  [USERS]  [GROUPS]  [TAGS]          │
│          │───────────────────────────────────────────────│
│          │                                               │
│          │  Содержимое активной вкладки                   │
│          │                                               │
└──────────────────────────────────────────────────────────┘
```

## 1. Header

| Элемент | Описание |
|---|---|
| Заголовок | «Настройки» (h2, 32px, 700) |
| Подзаголовок | «Управление пользователями, группами и тегами» (15px, text-secondary) |
| Кнопка «+ Добавить пользователя» | btn-primary. Видна только на вкладке USERS. Скрывается при переключении на GROUPS / TAGS |

## 2. Вкладки (.tabs)

Горизонтальная навигация под заголовком. Нижняя граница `1px solid border-color`. Активная вкладка выделяется зелёной нижней границей и цветом текста.

| Параметр | Значение |
|---|---|
| Шрифт | 14px, font-weight: 600, uppercase, letter-spacing: 0.5px |
| Отступы | padding: 14px 32px |
| Неактивная вкладка | color: text-secondary, border-bottom: 2px transparent |
| Hover | color: text-primary |
| Активная вкладка | color: accent-primary, border-bottom: 2px accent-primary |

### Список вкладок

| Вкладка | data-tab | Контент | Статус |
|---|---|---|---|
| USERS | `users` | `#tab-users` | Реализована |
| GROUPS | `groups` | `#tab-groups` | Заглушка |
| TAGS | `tags` | `#tab-tags` | Заглушка |

### UX-поведение вкладок

- Клик по вкладке: снимает `.active` со всех вкладок и контентов, ставит `.active` на выбранную
- При переключении с USERS кнопка «+ Добавить пользователя» скрывается (`display: none`)
- При возврате на USERS кнопка появляется обратно
- Контент неактивных вкладок скрыт через `display: none`

---

# Вкладка USERS (tab-users)

## Назначение

Управление пользователями системы: просмотр списка, фильтрация, групповое назначение в группы доступа, удаление. Визуально и функционально повторяет паттерн страницы `objects.html`.

## Структура вкладки

```
┌──────────────────────────────────────────────────────────┐
│  Фильтры (поиск, группа, кнопки Сбросить/Применить)     │
│─────────────────────────────────────────────────────────│
│  Bulk Toolbar (появляется при выборе ≥1)                │
│─────────────────────────────────────────────────────────│
│  Таблица пользователей                                   │
│  ┌─ ☐ │ Имя       │ Email            │ Группа │ Статус ┐│
│  │ ☐  │ Иванов А. │ a.ivanov@...     │ Adm    │ Active ││
│  │ ...│           │                  │        │        ││
│  └─────────────────────────────────────────────────────┘│
│  Пагинация: ← 1 2 3 →                                  │
└──────────────────────────────────────────────────────────┘

+ Контекстное меню (ПКМ на строке)
+ Модальное окно подтверждения удаления
```

## 3. Фильтры (.filters-section)

Панель фильтрации над таблицей. Сетка: `grid-template-columns: 2fr 1fr 1fr`.

| Фильтр | Тип | Описание |
|---|---|---|
| Поиск | `<input>` текстовое поле | Поиск по имени пользователя или email. Placeholder: «Имя пользователя или email...» |
| Группа | Кастомный dropdown с чекбоксами | Множественный выбор групп. По умолчанию: «Все группы». При выборе текст кнопки обновляется на список выбранных через запятую |
| Кнопки | «Сбросить» (secondary) + «Применить» (primary) | Прижаты к правому нижнему углу ячейки |

### Значения фильтра «Группа»

- Administrators
- Operators
- Engineers
- Guests

### UX-поведение фильтр-дропдаунов

- Клик по кнопке — открывает/закрывает выпадающий список
- Клик по чекбоксу внутри списка **не закрывает** дропдаун (stopPropagation)
- Клик в любом месте за пределами дропдауна — закрывает его
- Текст кнопки обновляется динамически: без выбора — «Все группы», при выборе — перечисление через запятую

## 4. Bulk Toolbar (.bulk-toolbar)

Панель групповых действий. Скрыта по умолчанию (`display: none`), появляется при выборе ≥ 1 строки в таблице с анимацией slideDown.

| Элемент | Описание |
|---|---|
| Счётчик | «Выбрано: N» (accent-primary, 14px, 600) |
| Разделитель | Вертикальная линия 1px × 24px |
| «Назначить группу» | Dropdown с множественным выбором групп + кнопки «Сбросить» / «Применить» |
| «Удалить» | btn-danger, открывает модальное окно подтверждения |

### UX-поведение bulk-дропдауна

- Клик внутри дропдауна (чекбоксы) **не закрывает** его
- «Сбросить» — сбрасывает все чекбоксы внутри дропдауна и закрывает его
- «Применить» — закрывает дропдаун (в mock без реального применения)
- Рамка тулбара — accent-primary (зелёная), выделяет панель визуально

### Значения дропдауна «Назначить группу»

- Administrators
- Operators
- Engineers
- Guests

## 5. Таблица пользователей (.data-table)

### Столбцы

| # | Столбец | CSS-класс | Описание |
|---|---|---|---|
| 1 | ☐ | `.th-checkbox` / `.td-checkbox` | Чекбокс выбора (48px, center) |
| 2 | Имя пользователя | `.user-name-cell` | ФИО (font-weight: 600) |
| 3 | Email | `.user-email-cell` | Email (JetBrains Mono, 12px, accent-secondary) |
| 4 | Группа | `.group-badge` | Бейджи групп (accent-secondary фон/рамка). У пользователя может быть несколько групп |
| 5 | Статус | `.status-badge` | Статус активности пользователя |

### Статусы пользователей

| Статус | CSS-класс | Цвет фона | Цвет текста |
|---|---|---|---|
| Active | `.status-badge--active` | rgba(0, 255, 136, 0.1) | accent-primary (#00ff88) |
| Inactive | `.status-badge--inactive` | rgba(139, 148, 168, 0.15) | text-secondary (#8b94a8) |

### Мок-данные (8 строк)

| Имя | Email | Группы | Статус |
|---|---|---|---|
| Иванов Алексей | a.ivanov@company.ru | Administrators | Active |
| Петрова Мария | m.petrova@company.ru | Operators | Active |
| Сидоров Дмитрий | d.sidorov@company.ru | Engineers | Active |
| Козлова Анна | a.kozlova@company.ru | Operators, Engineers | Active |
| Новиков Сергей | s.novikov@company.ru | Administrators, Operators | Active |
| Морозова Елена | e.morozova@company.ru | Guests | Inactive |
| Волков Андрей | a.volkov@company.ru | Engineers | Active |
| Лебедева Ольга | o.lebedeva@company.ru | Guests | Inactive |

### Интерактивность таблицы

- **Select All** (чекбокс в заголовке): выделяет/снимает все строки
- **Индивидуальный чекбокс**: выделяет строку, обновляет состояние Select All (checked / indeterminate)
- **Подсветка строки**: выбранные строки получают класс `.selected` (зеленоватый фон)
- **Анимация**: строки появляются с анимацией fadeIn с нарастающей задержкой (0.03s шаг)
- **Hover**: лёгкая подсветка строки (rgba(255,255,255, 0.02))

## 6. Пагинация (.table-footer)

| Элемент | Описание |
|---|---|
| Информация | «Показано 1-8 из 24 пользователей» (13px, text-secondary) |
| Кнопки | ← 1 2 3 → (page-btn, активная страница — accent-primary фон) |

## 7. Контекстное меню (.context-menu)

Появляется при правом клике (contextmenu) на строке таблицы. Позиционируется по координатам курсора.

| Пункт | ID | Действие |
|---|---|---|
| Изменить группу | `ctxChangeGroup` | Открывает bulk-дропдаун групп |
| ── разделитель ── | | |
| Удалить | `ctxDelete` | Открывает модальное окно удаления |

### UX-поведение контекстного меню

- Если строка **не выделена** — при ПКМ автоматически снимает выделение с остальных, выделяет текущую строку
- Если строка **уже выделена** — контекстное меню применяется ко всем выделенным строкам
- Закрывается при клике в любом месте

## 8. Модальное окно удаления (.modal-overlay + .modal)

| Элемент | Описание |
|---|---|
| Иконка | `!` на красноватом фоне (56×56px) |
| Заголовок | «Удалить пользователей?» |
| Текст | «Вы уверены, что хотите удалить выбранных пользователей (N шт.)? Это действие нельзя отменить.» — N подставляется динамически |
| «Отмена» | btn-secondary, закрывает модалку |
| «Удалить» | btn-danger, удаляет выбранные строки из DOM (mock) |

### UX-поведение модалки

- Открывается поверх всего (z-index: 1000) с полупрозрачным оверлеем
- Анимация появления: scale 0.95 → 1
- Закрытие: кнопка «Отмена» или клик по оверлею
- После подтверждения: строки удаляются из таблицы, чекбоксы сбрасываются, тулбар скрывается

---

# Вкладки-заглушки

## GROUPS (tab-groups)

Заглушка с текстом «Управление группами — Раздел находится в разработке». Иконка: 👥. Контент будет реализован по требованиям `docs/104_settings_groups.md`.

## TAGS (tab-tags)

Заглушка с текстом «Управление тегами — Раздел находится в разработке». Иконка: 🏷️. Контент будет реализован по требованиям `docs/104_settings_tags.md`.

---

# Стилевые параметры

## CSS-переменные (dark theme)

| Переменная | Значение | Назначение |
|---|---|---|
| `--bg-primary` | #0a0e1a | Фон body |
| `--bg-secondary` | #111827 | Фон карточек/таблицы |
| `--bg-tertiary` | #1a2332 | Фон полей ввода/thead |
| `--accent-primary` | #00ff88 | Основной акцент (зелёный) |
| `--accent-secondary` | #00ccff | Вторичный акцент (голубой) |
| `--accent-warning` | #ffaa00 | Предупреждение (оранжевый) |
| `--accent-danger` | #ff3366 | Опасность (красный) |
| `--text-primary` | #e4e8f0 | Основной текст |
| `--text-secondary` | #8b94a8 | Вторичный текст |
| `--border-color` | #1f2937 | Границы |

## Шрифты

| Шрифт | Использование |
|---|---|
| Outfit (300, 400, 600, 700) | Основной UI |
| JetBrains Mono (400, 500, 700) | Email-ячейки, моноширинный текст |

## z-index карта

| z-index | Элемент |
|---|---|
| 0 | body::before (градиентный фон) |
| 1 | .container |
| 50 | Dropdown-меню, фильтр-дропдауны |
| 100 | Sidebar |
| 999 | Контекстное меню |
| 1000 | Модальное окно |

---

# Подключение

```html
<aside class="sidebar"></aside>
<!-- ... основной контент ... -->
<script src="../components/sidebar.js"></script>
<script>
    // Вся логика страницы в IIFE
    (function () { ... })();
</script>
```

Сайдбар подключается как внешний компонент (`components/sidebar.js` + `components/sidebar.css`). Вся остальная логика — inline-скрипт в конце body.

---

# Отличия от страницы объектов (objects.html)

| Аспект | objects.html | settings.html (USERS) |
|---|---|---|
| Навигация | Нет вкладок | Вкладки USERS / GROUPS / TAGS |
| Столбцы таблицы | Название, URL, Группа, Теги | Имя, Email, Группа, Статус |
| Фильтры | Поиск + Группа + Тег | Поиск + Группа (без фильтра по тегу) |
| Bulk-действия | Изменить группу, Изменить теги, Удалить | Назначить группу, Удалить |
| Контекстное меню | Изменить группу, Изменить теги, Удалить | Изменить группу, Удалить |
| Кнопка заголовка | «+ Добавить объект» | «+ Добавить пользователя» (скрывается на других вкладках) |
| Пагинация | 142 объекта, 18 страниц | 24 пользователя, 3 страницы |
