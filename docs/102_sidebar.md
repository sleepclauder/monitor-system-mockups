# 102 — Sidebar (Боковая панель)

## Файлы компонента

| Файл | Назначение |
|---|---|
| `components/sidebar.js` | Генерация HTML-разметки сайдбара |
| `components/sidebar.css` | Стили сайдбара |

## Структура

Сайдбар — фиксированная вертикальная панель шириной **72px** по левому краю экрана. Состоит из трёх блоков:

```
┌──────────┐
│   M//S   │  <- Логотип
│──────────│
│   🖥️    │  <- Навигация (nav-menu)
│   ⚙️    │
│          │
│          │  <- Пустое пространство (margin-top: auto)
│──────────│
│   🌐    │  <- Язык
│   👤    │  <- Профиль
│   🚪    │  <- Выход
└──────────┘
```

### 1. Логотип (.logo)
- Текст: `M//S`
- Шрифт: JetBrains Mono, 14px, bold
- Градиентная заливка текста (accent-primary -> accent-secondary)
- Отделён от навигации нижней границей

### 2. Навигация (.nav-menu)
Массив `navItems` в `sidebar.js`. Каждый элемент содержит:

| Параметр | Тип | Описание |
|---|---|---|
| `href` | string | Ссылка на страницу |
| `icon` | string | Emoji-иконка |
| `label` | string | Текст тултипа при наведении |

#### Активные пункты меню

| Страница | Иконка | Статус |
|---|---|---|
| objects.html | 🖥️ | Активен |
| settings.html | ⚙️ | Активен |

#### Скрытые пункты (закомментированы)

| Страница | Иконка | Причина |
|---|---|---|
| dashboard.html | 📊 | Временно скрыт |
| object-detail.html | 📝 | Временно скрыт |
| alerts.html | 🔔 | Временно скрыт |
| reports.html | 📈 | Временно скрыт |

Текущая страница определяется автоматически по `location.pathname` и получает класс `.active` (подсветка accent-primary, зелёная полоса слева).

### 3. Нижний блок (.sidebar-bottom)
Прижат к низу через `margin-top: auto`. Отделён верхней границей. Порядок кнопок (сверху вниз):

| ID | Иконка | Тултип | Назначение |
|---|---|---|---|
| `lang-toggle` | 🌐 | Язык | Переключение языка системы |
| `user-btn` | 👤 | Профиль | Профиль пользователя |
| `logout-btn` | 🚪 | Выход | Выход из системы |

Кнопка выхода имеет модификатор `sidebar-bottom-btn--logout` — при наведении подсвечивается красным (#ff4d4f).

## Стилевые параметры

| Параметр | Значение |
|---|---|
| Ширина панели | 72px |
| Позиционирование | fixed, left: 0, top: 0, height: 100vh |
| z-index панели | 100 |
| z-index тултипов | 200 |
| Фон | var(--bg-secondary) |
| Размер иконок навигации | 22px |
| Размер иконок нижнего блока | 18px |
| Отступ элементов навигации | padding: 14px 0 |
| Отступ нижних кнопок | padding: 12px 0 |

## Подключение

На каждой странице в HTML должен быть элемент `.sidebar`, а скрипт подключается в конце body:

```html
<aside class="sidebar"></aside>
<!-- ... -->
<script src="../components/sidebar.js"></script>
```

Скрипт выполняется как IIFE и заполняет `.sidebar` сгенерированной разметкой.
