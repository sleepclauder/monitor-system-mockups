# Git Шпаргалка - Быстрый справочник

## 🚀 Первая загрузка проекта на GitHub

```bash
# 1. Перейти в папку проекта
cd monitor-system-mockups

# 2. Инициализировать Git
git init

# 3. Добавить все файлы
git add .

# 4. Сделать первый коммит
git commit -m "Initial commit"

# 5. Подключить GitHub (замените YOUR_USERNAME!)
git remote add origin https://github.com/YOUR_USERNAME/monitor-system-mockups.git

# 6. Переименовать ветку в main
git branch -M main

# 7. Загрузить на GitHub
git push -u origin main
```

---

## 📝 Ежедневная работа (самое важное!)

```bash
# УТРОМ: Получить изменения коллег
git pull

# Работайте с файлами...

# ВЕЧЕРОМ: Загрузить свои изменения
git add .
git commit -m "Описание что сделали"
git push
```

---

## 🔄 Полный цикл изменений

```bash
# 1. Проверить статус (что изменено)
git status

# 2. Посмотреть конкретные изменения
git diff

# 3. Добавить файлы для коммита
git add .                    # Все файлы
git add filename.html        # Конкретный файл

# 4. Создать коммит с описанием
git commit -m "Добавлена новая страница"

# 5. Загрузить на GitHub
git push
```

---

## 📊 Просмотр информации

```bash
git status          # Текущий статус (что изменено)
git log             # История коммитов
git log --oneline   # Краткая история
git diff            # Что изменено (детально)
```

---

## ⚠️ Частые проблемы и решения

### Забыли сделать git pull перед работой

```bash
# Получить изменения
git pull

# Если возникли конфликты - смотрите ниже
```

### Конфликт при git pull

```bash
# 1. Git покажет какие файлы конфликтуют
# 2. Откройте эти файлы
# 3. Найдите и исправьте блоки:
#    <<<<<<< HEAD
#    ваша версия
#    =======
#    версия коллеги
#    >>>>>>> abc123
# 4. Удалите маркеры, оставьте правильный код
# 5. Сохраните и выполните:

git add .
git commit -m "Разрешены конфликты"
git push
```

### Случайно изменили не тот файл

```bash
# Отменить изменения (до git add)
git checkout -- filename.html

# Отменить все изменения
git checkout -- .
```

### Хотите отменить последний коммит

```bash
# Отменить коммит, но сохранить изменения
git reset --soft HEAD~1

# Теперь можно заново сделать git add и commit
```

---

## 🔐 Аутентификация

### При использовании HTTPS

Вместо пароля используйте **Personal Access Token**:

1. GitHub → Settings → Developer settings
2. Personal access tokens → Generate new token
3. Выберите `repo` права
4. Скопируйте токен (ghp_xxxx...)
5. Используйте вместо пароля при git push

### При использовании SSH

```bash
# Генерация ключа (один раз)
ssh-keygen -t ed25519 -C "your@email.com"

# Копировать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавить на GitHub:
# Settings → SSH and GPG keys → New SSH key

# Изменить remote на SSH
git remote set-url origin git@github.com:USERNAME/repo.git
```

---

## 👥 Работа в команде

### Получить изменения коллег

```bash
git pull
```

### Посмотреть кто что изменил

```bash
git log --oneline
git log --author="Имя"
```

### Посмотреть изменения в конкретном коммите

```bash
git show abc123
```

---

## 🌿 Работа с ветками (advanced)

```bash
# Создать новую ветку
git checkout -b feature/new-page

# Посмотреть все ветки
git branch

# Переключиться на другую ветку
git checkout main

# Объединить ветку с текущей
git merge feature/new-page

# Удалить ветку
git branch -d feature/new-page
```

---

## 📦 Клонирование проекта

### Скачать проект коллеги

```bash
git clone https://github.com/USERNAME/repository.git
cd repository
```

---

## 🛠️ Настройки (один раз)

```bash
# Представиться Git
git config --global user.name "Ваше Имя"
git config --global user.email "email@example.com"

# Посмотреть все настройки
git config --list
```

---

## ❌ Что НЕ надо делать

- ❌ `git push --force` (кроме особых случаев)
- ❌ Коммитить большие файлы (>100MB)
- ❌ Коммитить пароли и токены
- ❌ Работать без git pull перед началом
- ❌ Коммитить `node_modules/` или временные файлы

---

## ✅ Хорошие практики

- ✅ Делайте `git pull` перед началом работы
- ✅ Коммитьте часто (каждое логическое изменение)
- ✅ Пишите понятные commit messages
- ✅ Делайте `git push` в конце дня
- ✅ Проверяйте `git status` перед коммитом
- ✅ Используйте `.gitignore` для исключения файлов

---

## 📱 Графические программы (альтернатива командам)

- **GitHub Desktop** - https://desktop.github.com
- **GitKraken** - https://www.gitkraken.com
- **Sourcetree** - https://www.sourcetreeapp.com
- **VS Code** - встроенная Git поддержка

---

## 🆘 Помощь

```bash
# Справка по команде
git help command
git command --help

# Примеры
git help commit
git push --help
```

---

## 📞 Экстренная помощь

### Все сломалось, хочу начать заново

```bash
# Скопируйте свои изменения в другое место!

# Удалить все локальные изменения
git reset --hard HEAD
git clean -fd

# Получить свежую копию
git pull
```

### Случайно удалил файл

```bash
git checkout HEAD -- filename.html
```

### Нужно вернуться к старой версии

```bash
# Посмотреть историю
git log --oneline

# Временно перейти к старому коммиту
git checkout abc123

# Вернуться к последнему
git checkout main
```

---

## 🎓 Полезные ресурсы

- Полное руководство: `docs/GITHUB-FOR-BEGINNERS.md`
- [GitHub Docs](https://docs.github.com)
- [Git Book](https://git-scm.com/book/ru/v2)
- [Интерактивный туториал](https://learngitbranching.js.org/?locale=ru_RU)

---

## 💡 Совет дня

> Не бойтесь экспериментировать! Git хранит всю историю - вы всегда можете откатиться.

**Золотое правило:** 
```
git pull → работа → git add → git commit → git push
```

---

Распечатайте эту шпаргалку и держите под рукой! 📋
