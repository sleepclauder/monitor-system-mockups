# Быстрый старт

Это руководство поможет вам за 5 минут начать работу с мокапами MONITOR//SYS.

## Содержание
- [Вариант 1: Открыть в браузере](#вариант-1-открыть-в-браузере)
- [Вариант 2: Локальный сервер](#вариант-2-локальный-сервер)
- [Вариант 3: VS Code Live Server](#вариант-3-vs-code-live-server)
- [Навигация](#навигация)
- [Редактирование](#редактирование)

## Вариант 1: Открыть в браузере

Самый простой способ - открыть файлы напрямую:

1. Скачайте или клонируйте репозиторий
2. Найдите файл `pages/dashboard.html`
3. Дважды кликните по нему или перетащите в браузер

**Ограничения:** Навигация между страницами будет работать, но некоторые браузеры могут показывать предупреждения о локальных файлах.

## Вариант 2: Локальный сервер

Рекомендуемый способ для полноценной работы.

### С помощью Python (встроен в macOS/Linux)

```bash
# Перейдите в директорию проекта
cd monitor-system-mockups

# Запустите сервер
python3 -m http.server 8000

# Откройте в браузере
# http://localhost:8000
```

### С помощью Node.js

```bash
# Установите http-server (один раз)
npm install -g http-server

# Запустите
cd monitor-system-mockups
http-server -p 8000

# Откройте в браузере
# http://localhost:8000
```

### С помощью PHP

```bash
cd monitor-system-mockups
php -S localhost:8000

# Откройте в браузере
# http://localhost:8000
```

### С помощью npm scripts (если установлен Node.js)

```bash
cd monitor-system-mockups

# Запуск простого сервера
npm start

# Или с автоматическим обновлением (требует browser-sync)
npm run dev
```

## Вариант 3: VS Code Live Server

Если вы используете Visual Studio Code:

1. Установите расширение "Live Server" от Ritwick Dey
2. Откройте папку проекта в VS Code
3. Правый клик на `index.html` → "Open with Live Server"
4. Или нажмите "Go Live" в статус-баре

**Преимущества:** Автоматическое обновление при сохранении файлов.

## Навигация

После запуска вы увидите 6 основных страниц:

1. **Dashboard** (`/pages/dashboard.html`)
   - Главная панель с общей статистикой
   - Начальная точка входа

2. **Объекты мониторинга** (`/pages/objects.html`)
   - Список всех серверов
   - Фильтры и поиск

3. **Детали объекта** (`/pages/object-detail.html`)
   - Подробная информация о сервере
   - Графики и логи

4. **Алерты** (`/pages/alerts.html`)
   - Уведомления и события
   - Управление инцидентами

5. **Отчеты** (`/pages/reports.html`)
   - Аналитика и графики
   - Шаблоны отчетов

6. **Настройки** (`/pages/settings.html`)
   - Конфигурация системы
   - API ключи

Используйте боковую панель (sidebar) для перехода между страницами.

## Редактирование

### Изменение цветов

Откройте любой HTML файл и найдите секцию `:root`:

```css
:root {
    --bg-primary: #0a0e1a;         /* Основной фон */
    --accent-primary: #00ff88;     /* Зеленый акцент */
    --accent-secondary: #00ccff;   /* Синий акцент */
    /* ... другие цвета */
}
```

Измените значения и сохраните файл. Обновите страницу в браузере.

### Изменение текста

Просто найдите нужный текст в HTML и измените его:

```html
<h2>Общий обзор системы</h2>
<!-- Измените на -->
<h2>Мой заголовок</h2>
```

### Добавление новых элементов

Скопируйте существующий блок и модифицируйте:

```html
<!-- Скопируйте stat-card -->
<div class="stat-card">
    <div class="stat-header">
        <div class="stat-icon success">✓</div>
        <span class="stat-trend up">+2.3%</span>
    </div>
    <div class="stat-value">127</div>
    <div class="stat-label">Ваша метрика</div>
</div>
```

## Горячие клавиши (в браузере)

- `Ctrl/Cmd + R` - Обновить страницу
- `F12` - Открыть DevTools
- `Ctrl/Cmd + Shift + C` - Инспектор элементов
- `Ctrl/Cmd + Shift + M` - Режим адаптивного дизайна

## Советы

### Работа с DevTools

1. Нажмите `F12` для открытия инструментов разработчика
2. Выберите элемент с помощью инспектора
3. В правой панели можете:
   - Изменять CSS в реальном времени
   - Смотреть все примененные стили
   - Тестировать разные значения

### Использование Git

```bash
# Первый раз
git clone <repository-url>
cd monitor-system-mockups

# Для обновления
git pull origin main

# Для сохранения изменений
git add .
git commit -m "Описание изменений"
git push origin main
```

### Экспорт в PDF

Для создания PDF документации:

1. Откройте страницу в браузере
2. `Ctrl/Cmd + P` (Печать)
3. Выберите "Сохранить как PDF"
4. Настройте параметры (фон, масштаб)
5. Сохраните

## Следующие шаги

1. Изучите [README.md](../README.md) для полной документации
2. Прочитайте [CONTRIBUTING.md](../CONTRIBUTING.md) перед внесением изменений
3. Посмотрите [CHANGELOG.md](../CHANGELOG.md) для истории версий

## Частые вопросы

**В: Страницы не открываются / белый экран**
О: Убедитесь что вы запустили локальный сервер, а не просто открыли файл.

**В: Навигация не работает**
О: Проверьте что вы находитесь в корне проекта при запуске сервера.

**В: Как изменить порт сервера?**
О: Для Python: `python3 -m http.server 3000` (замените 3000 на нужный порт)

**В: Шрифты не загружаются**
О: Требуется подключение к интернету для загрузки Google Fonts.

**В: Можно ли использовать без интернета?**
О: Да, но нужно скачать шрифты локально и обновить ссылки в HTML.

## Помощь

Если возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Создайте Issue на GitHub
3. Свяжитесь с командой разработки

---

**Готово!** Теперь вы можете работать с мокапами. Удачи! 🚀
