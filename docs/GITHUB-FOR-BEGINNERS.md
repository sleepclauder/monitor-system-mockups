# GitHub для начинающих: Пошаговая инструкция

Это руководство для тех, кто впервые работает с GitHub. Мы пройдем все шаги от создания репозитория до размещения ваших мокапов онлайн.

## 📋 Содержание

1. [Что такое GitHub и зачем он нужен](#что-такое-github)
2. [Установка необходимых программ](#установка-программ)
3. [Создание репозитория на GitHub](#создание-репозитория)
4. [Загрузка файлов проекта](#загрузка-файлов)
5. [Публикация на GitHub Pages](#github-pages)
6. [Приглашение команды](#приглашение-команды)
7. [Ежедневная работа](#ежедневная-работа)
8. [Решение проблем](#решение-проблем)

---

## Что такое GitHub?

**GitHub** - это сайт для хранения кода и совместной работы над проектами.

### Зачем он нужен:
- ✅ **Хранение версий** - вся история изменений сохраняется
- ✅ **Совместная работа** - команда работает вместе без конфликтов
- ✅ **Резервное копирование** - файлы в облаке, не потеряются
- ✅ **Бесплатный хостинг** - можно выложить сайт онлайн бесплатно
- ✅ **Отслеживание изменений** - кто, что и когда изменил

### Основные понятия:

- **Repository (Репозиторий)** = Папка с проектом
- **Commit (Коммит)** = Сохранение изменений с описанием
- **Push (Пуш)** = Отправка изменений на GitHub
- **Pull (Пулл)** = Скачивание изменений с GitHub
- **Branch (Ветка)** = Параллельная версия проекта

---

## Установка программ

### Шаг 1: Установите Git

Git - это программа для работы с версиями файлов.

#### На Windows:

1. Перейдите на [git-scm.com/download/win](https://git-scm.com/download/win)
2. Скачайте установщик (64-bit)
3. Запустите установщик
4. При установке выберите:
   - ✅ Git Bash Here
   - ✅ Git from the command line and also from 3rd-party software
5. Остальное оставьте по умолчанию
6. Нажимайте "Next" → "Install" → "Finish"

#### На macOS:

Откройте Terminal (⌘ + Space → введите "Terminal"):

```bash
# Проверьте, установлен ли Git
git --version

# Если нет, macOS предложит установить
# Или установите через Homebrew:
brew install git
```

#### На Linux:

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install git

# Fedora
sudo dnf install git
```

### Шаг 2: Проверьте установку

Откройте командную строку и выполните:

```bash
git --version
```

Должно вывести: `git version 2.XX.X`

### Шаг 3: Настройте Git

Выполните в командной строке:

```bash
# Укажите ваше имя
git config --global user.name "Ваше Имя"

# Укажите email (тот же, что на GitHub)
git config --global user.email "your.email@example.com"
```

**Пример:**
```bash
git config --global user.name "Ivan Petrov"
git config --global user.email "ivan.petrov@company.com"
```

---

## Создание репозитория

### Шаг 1: Войдите на GitHub

1. Откройте [github.com](https://github.com)
2. Нажмите "Sign in"
3. Введите логин и пароль

### Шаг 2: Создайте новый репозиторий

1. В правом верхнем углу нажмите **"+"** → **"New repository"**
   
2. Заполните форму:

   **Repository name**
   ```
   monitor-system-mockups
   ```
   
   **Description**
   ```
   Интерактивные HTML мокапы системы мониторинга IT-инфраструктуры
   ```
   
   **Видимость:**
   - **Public** - все могут видеть
   - **Private** - только вы и приглашенные
   
   **ВАЖНО - НЕ СТАВЬТЕ галочки:**
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license

3. Нажмите **"Create repository"**

### Шаг 3: Скопируйте URL

После создания увидите страницу с инструкциями.

**Найдите и скопируйте URL:**
```
https://github.com/ВАШ_USERNAME/monitor-system-mockups.git
```

**Сохраните этот URL!**

---

## Загрузка файлов

### Шаг 1: Распакуйте архив

1. Скачайте `monitor-system-mockups.zip`
2. Распакуйте в удобное место, например:
   - Windows: `C:\Projects\monitor-system-mockups`
   - macOS: `/Users/ваше_имя/Projects/monitor-system-mockups`
   - Linux: `/home/ваше_имя/Projects/monitor-system-mockups`

### Шаг 2: Откройте командную строку в папке

**Windows (простой способ):**
1. Откройте папку в Проводнике
2. В адресной строке наберите `cmd` и нажмите Enter

**macOS/Linux:**
```bash
cd /путь/к/папке/monitor-system-mockups
```

**Проверка:**
```bash
# Windows
dir

# macOS/Linux
ls
```

Должны увидеть: `README.md`, `index.html`, папки `pages`, `docs`

### Шаг 3: Выполните команды Git

Скопируйте и выполняйте **по одной** команде:

```bash
# 1. Инициализация Git
git init
```

**Что произошло:** Создана скрытая папка `.git`

```bash
# 2. Добавление файлов
git add .
```

**Что произошло:** Все файлы подготовлены для сохранения

```bash
# 3. Первое сохранение (коммит)
git commit -m "Initial commit: добавлены все мокапы v1.0.0"
```

**Что произошло:** Создан снимок проекта

```bash
# 4. Подключение к GitHub
# ЗАМЕНИТЕ ВАШ_USERNAME на ваш логин!
git remote add origin https://github.com/ВАШ_USERNAME/monitor-system-mockups.git
```

**Пример:**
```bash
git remote add origin https://github.com/ivanpetrov/monitor-system-mockups.git
```

```bash
# 5. Переименование ветки
git branch -M main
```

```bash
# 6. Отправка на GitHub
git push -u origin main
```

### Шаг 4: Авторизация

При первом push GitHub попросит авторизоваться.

**ВАЖНО:** Вместо пароля нужен **Personal Access Token**

#### Создание токена:

1. GitHub → **ваш аватар** (правый верх) → **Settings**
2. Внизу слева → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. Заполните:
   - **Note:** `monitor-system-mockups`
   - **Expiration:** 90 days
   - **Галочки:** ✅ **repo** (все подпункты)
6. **Generate token**
7. **СКОПИРУЙТЕ ТОКЕН!** Он больше не покажется!

#### Использование:

```
Username: ваш_логин_github
Password: вставьте_скопированный_токен
```

**После успешной отправки:**
```
To https://github.com/ВАШ_USERNAME/monitor-system-mockups.git
 * [new branch]      main -> main
```

### Шаг 5: Проверьте

Откройте в браузере:
```
https://github.com/ВАШ_USERNAME/monitor-system-mockups
```

Вы должны увидеть все файлы!

**🎉 Поздравляю! Проект на GitHub!**

---

## GitHub Pages

Сделаем мокапы доступными онлайн.

### Шаг 1: Откройте Settings

В репозитории нажмите **Settings** (вверху)

### Шаг 2: Настройте Pages

1. Слева найдите **Pages**
2. **Source:**
   - Branch: `main`
   - Folder: `/ (root)`
3. **Save**

### Шаг 3: Дождитесь публикации

- Подождите 1-2 минуты
- Обновите страницу
- Появится зеленая плашка:
  ```
  Your site is live at https://ВАШ_USERNAME.github.io/monitor-system-mockups/
  ```

### Шаг 4: Откройте сайт

Перейдите по ссылке - мокапы онлайн!

**Поделитесь этой ссылкой с командой!**

---

## Приглашение команды

### Добавление коллег:

1. **Settings** → **Collaborators**
2. **Add people**
3. Введите username или email
4. **Add to repository**

Коллеги получат приглашение по email.

### Права соавторов:

По умолчанию могут:
- ✅ Просматривать код
- ✅ Клонировать
- ✅ Создавать ветки
- ✅ Отправлять изменения

---

## Ежедневная работа

### Сценарий: Изменение файла

```bash
# 1. Получите последние изменения
git pull origin main

# 2. Измените файл в редакторе
# Например: pages/dashboard.html

# 3. Проверьте изменения
git status

# 4. Добавьте файл
git add pages/dashboard.html

# 5. Сохраните с описанием
git commit -m "fix: исправлена статистика"

# 6. Отправьте на GitHub
git push origin main
```

**Изменения автоматически появятся на сайте!**

### Получение изменений коллег:

```bash
git pull origin main
```

---

## Решение проблем

### Проблема: "fatal: not a git repository"

**Решение:** Перейдите в папку проекта
```bash
cd путь/к/monitor-system-mockups
```

### Проблема: "Permission denied"

**Решение:** Проверьте токен, создайте новый

### Проблема: "Updates were rejected"

**Решение:** Сначала получите изменения
```bash
git pull origin main
git push origin main
```

### Проблема: GitHub Pages не работает

1. Проверьте Settings → Pages
2. Убедитесь что Source = `main` / `root`
3. Подождите 5 минут

---

## Шпаргалка команд

```bash
# Проверка статуса
git status

# Добавить файлы
git add .

# Сохранить
git commit -m "Описание"

# Отправить
git push origin main

# Получить изменения
git pull origin main

# История
git log --oneline

# Отменить изменения в файле
git checkout -- файл.html
```

---

## Дополнительные ресурсы

- [GitHub Docs](https://docs.github.com/ru) - официальная документация
- [Git Book](https://git-scm.com/book/ru/v2) - полная книга на русском
- [GitHub Desktop](https://desktop.github.com) - визуальный клиент (без команд)

---

## FAQ

**Q: Нужно платить?**  
A: Нет, GitHub бесплатен

**Q: Можно без командной строки?**  
A: Да, используйте [GitHub Desktop](https://desktop.github.com)

**Q: Как удалить репозиторий?**  
A: Settings → Danger Zone → Delete

**Q: Потерял токен, что делать?**  
A: Создайте новый (см. Шаг 4 выше)

---

**Готово! Теперь вы можете работать с GitHub! 🚀**

Если что-то не получается - перечитайте инструкцию внимательно или спросите у коллег.

**Удачи!**
